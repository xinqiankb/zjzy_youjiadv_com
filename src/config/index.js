// 全局请求url
const config = {
	url: '', // request url
	imgUrl: '' // pic path autocomplete
}

// pagechange config
const page = {
	limit: 10,
	page: 1
}

// 每页显示条数
const limit = 1

export default {config, page, limit}